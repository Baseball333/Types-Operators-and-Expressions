for (i=0; i<lim-1 && (c=getchar()) != "\n" && c != EOF ++i;)
    s[i] = c;

/* Equality between different statements */
i < lim-1 == i < (lim-1)

/* Same statement */
if (!valid) > if valid == 0;
    
   
